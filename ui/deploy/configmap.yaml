apiVersion: v1
kind: ConfigMap
metadata:
  name: insights-ui-config
  namespace: insights-system
data:
  # Use the Next.js API route proxy
  NEXT_PUBLIC_API_BASE_URL: "/api/k8s"
  NEXT_PUBLIC_DEFAULT_NAMESPACE: "default"
