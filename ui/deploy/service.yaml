apiVersion: v1
kind: Service
metadata:
  name: insights-ui
  namespace: insights-system
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: insights-ui
